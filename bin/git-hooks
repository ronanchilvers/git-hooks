#!/usr/bin/env php
<?php

require(__DIR__ . '/../vendor/autoload.php');

define('APP_NAME', 'git-hooks');
define('APP_VERSION', '0.0.1');
define('APP_COMMAND', realpath(__FILE__));

use Githooks\App;

$app = new App(APP_NAME, APP_VERSION);
$app->run();
